diff --git a/src/os/unix/consts.rs b/src/os/unix/consts.rs
--- a/src/os/unix/consts.rs
+++ b/src/os/unix/consts.rs
@@ -66,6 +66,7 @@ mod posix {
             pub(super) const RTLD_LAZY: c_int = 4;
         } else if #[cfg(any(
             target_os = "linux",
+            target_os = "makos",
             target_os = "android",
             target_os = "emscripten",
 
@@ -102,6 +103,7 @@ mod posix {
             pub(super) const RTLD_NOW: c_int = 1;
         } else if #[cfg(any(
             target_os = "linux",
+            target_os = "makos",
             all(target_os = "android", target_pointer_width = "64"),
             target_os = "emscripten",
 
@@ -162,6 +164,7 @@ mod posix {
             pub(super) const RTLD_GLOBAL: c_int = 8;
         } else if #[cfg(any(
             target_os = "linux",
+            target_os = "makos",
             all(target_os = "android", target_pointer_width = "64"),
             target_os = "emscripten",
 
@@ -209,6 +212,7 @@ mod posix {
             pub(super) const RTLD_LOCAL: c_int = 4;
         } else if #[cfg(any(
             target_os = "linux",
+            target_os = "makos",
             target_os = "android",
             target_os = "emscripten",
